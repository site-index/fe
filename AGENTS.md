# Frontend (`fe/`)

- **Stack**: Vite, React, TypeScript.
- **Package root**: this directory (`package.json` here).
- **Dev**: `npm run dev` — **lint/typecheck/tests**: `npm run full-check`.
- Shared Cursor rules live under `../work-files/cursor/` (also linked as `fe/.cursor`).
